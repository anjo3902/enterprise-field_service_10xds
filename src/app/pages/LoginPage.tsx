import { LoginScreen } from "../components/LoginScreen";

/**
 * LoginPage — renders the LoginScreen.
 * Navigation (Sign In, Forgot Password, Create Account) is handled inside
 * LoginScreen via useNavigate (see component modifications).
 */
export default function LoginPage() {
  return <LoginScreen />;
}
